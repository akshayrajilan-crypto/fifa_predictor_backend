spring.application.name=Fifa

# PostgreSQL (local — using existing database)
spring.datasource.url=jdbc:postgresql://localhost:5432/iaps_platform_dev
spring.datasource.username=iaps
spring.datasource.password=iaps
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
app.jwt.secret=YourSuperSecretKeyForJWTTokenGenerationMustBeLongEnough256Bits!!
app.jwt.expiration-ms=86400000

# Football Data API
app.football-api.token=9eed3a122bdc4af49f52d111896901fa

# Server
server.port=8080
spring.jackson.time-zone=Asia/Kolkata
